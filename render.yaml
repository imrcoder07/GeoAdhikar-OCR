services:
  - type: web
    name: geo-adhikar-ocr
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false  # Set this in Render dashboard with your actual API key
      - key: FLASK_ENV
        value: production
    healthCheckPath: /
    plan: starter
